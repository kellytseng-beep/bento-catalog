# 便當目錄

淡水／淡江大學周邊便當店清單，單一 HTML 檔、無需建置。

- 線上瀏覽：GitHub Pages（見 repo Settings → Pages）
- 本地開啟：直接用瀏覽器打開 `index.html`

## 新增店家

編輯 `index.html` 裡 `<script>` 中的 `shops` 陣列，複製最下面的「範本」物件貼在陣列最後即可，畫面會自動產生卡片、地圖與快速索引。

## 備註

快速索引、店家編號與頁尾店數都由 shops 陣列自動產生，增刪店家後不需要另外調整。
